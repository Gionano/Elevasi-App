Letakkan file APK terbaru di folder ini dengan nama:

`elevasi-latest.apk`

File tersebut akan di-host oleh FastAPI pada path:

`/downloads/elevasi-latest.apk`

Endpoint metadata update tersedia di:

`/check-update`
